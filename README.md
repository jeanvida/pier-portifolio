
  # Premium PDF Portfolio Layout

  This is a code bundle for Premium PDF Portfolio Layout. The original project is available at https://www.figma.com/design/8unHomsrytzGVxpeGUrT8E/Premium-PDF-Portfolio-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  